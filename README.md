# dsh-mpkg-wallpaper — DSH 壁纸引擎 mpkg 背景插件 (v3)

给 DeepSeek Harness Web 界面（dsh web）添加背景壁纸的客户端插件，**支持直接在浏览器里解析 Wallpaper Engine / 麻匪动态壁纸的 `.mpkg` 文件**：

- 选择 `.mpkg` 文件 → 浏览器内解析容器 → 自动提取 `preview.gif`（壁纸的动态预览）作为**动态背景**（用 `<img>` 元素承载，GIF 动画可靠循环播放）；视频类壁纸自动播放内嵌 mp4
- 自动按**当前时间**选择素材（若壁纸包内含 `preview_night.gif` / `preview_day.gif` 等多时段素材）
- **可调参数编辑**：解析 `project.json → general.properties`，在设置页直接改（布尔/下拉/滑块/文本），保存在当前浏览器
- **界面虚化（磨砂）**：磨砂模糊条（整张壁纸）、统一虚化（开关+整屏虚化条，聊天区可独立选择是否跟随）、对话框/弹层/输入框独立虚化、设置遮罩虚化，全部可独立调节
- 侧边栏透出壁纸开关、镜头缩放（10–500）、镜头平移、面板不透明度、轻度锐化、Deep diving 背景框开关、时钟
- **冲突检测**：检测到其他壁纸/主题插件（含运行时背景检测）自动关闭本功能，避免叠加冲突
- **第三方插件共存**：与侧边栏/设置页类插件（DSH-better-sidebar、dsh-chat-import、
  dsh-sidebar-qa、dsh-plugin-account-balance 等）共存无冲突——本插件 CSS 只命中 DSH 原生
  区域类名，不覆盖插件注入内容；注入侧边栏的第三方内容自动获得 45% 雾底保证在壁纸上可读
- 设置页位于 **设置 → 壁纸引擎背景**（左侧导航，中性图标）
- 所有设置持久化在浏览器 localStorage；大文件（图片/GIF/视频/内嵌 mp4）自动存入浏览器 IndexedDB，刷新不丢失

## 功能与设置分组

- **背景来源**：总开关、mpkg 文件、图片链接、本地图片/动图
- **外观**：面板不透明度（统一虚化时同时控制所有区域白雾厚度）、磨砂模糊、镜头缩放、镜头位置
- **界面虚化**：统一虚化程度、对话框/弹层虚化开关 + 程度、设置遮罩虚化 + 程度、Deep diving 背景框、标题栏磨砂/透出壁纸
- **其他**：侧边栏透出壁纸、轻度锐化、恢复默认

## 最新反馈落实情况（第三轮）

| # | 反馈 | 处理 |
|---|---|---|
| ①④ | 标题栏不透出壁纸时应纯白不透明 | 关闭「标题栏透出壁纸」→ 亮色主题纯白 / 暗色主题纯深色，不再透出壁纸 |
| ② | 遮罩虚化条失效（对话框条反而控制遮罩） | 对话框虚化只作用于弹层面板/输入框；全屏背景遮罩虚化完全由「设置背景遮罩虚化」条独立控制，互不干扰 |
| ③ | 背景被分裂成"一块白一块透" | 新增「统一虚化程度」开关（默认开）：侧边栏/聊天区/标题栏/输入框/状态条白雾厚度全部跟随"面板不透明度"，视觉一致 |
| ⑤ | 设置项命名/归类 | 「顶部标题栏渲染背景图」→「标题栏透出壁纸」（与侧边栏透出壁纸对齐）；同类功能集中到「界面虚化」组 |
| ⑨ | UI 上 IndexedDB 字样难懂 | 设置页文案简化，IndexedDB 存储细节移入本文档 |
| ⑪ | 与其他壁纸插件冲突 | 冲突检测升级：除插件 ID 外增加运行时检测（body/html 背景图、其他全屏背景层）；检测到即自动关闭本功能（手动强开可豁免） |

## 最新反馈落实情况（第四轮）

| # | 反馈 | 处理 |
|---|---|---|
| ① | 统一虚化下聊天区跟随 | 新增「聊天壁纸跟随整屏」开关：开=跟随整屏虚化条；关=聊天区由磨砂模糊条接管 |
| ② | 各处注明用途 | 外观/界面虚化/透出壁纸三组各加说明；mpkg 选择处注明也可选 mp4/webm |
| ③ | 界面优化 | 每项滑条/镜头位置旁加「默认」按钮一键恢复；单位紧跟数字框（不再隔得远） |
| ④ | 输入框聚焦黄框 | 聚焦改为灰框→主题深色框，去掉浏览器默认黄/蓝 outline |
| ⑤ | 镜头缩放单位 | 加 % |
| ⑥ | 重置壁纸参数 | 只在有可调参数的壁纸时显示，与「展开全部」同排（折叠区内） |
| ⑦ | 时段残留 | 切到无时间变化壁纸时清掉上个壁纸的时段配置；只有带时间变化的壁纸才显示「当前时段」 |
| ⑨ | 大 mpkg 导入失败 | 按需读取（不整体进内存）+ 600MB 上限 + 报错友好化（存储不足/内存不足/解析失败分别提示） |

## 最新反馈落实情况（第二轮）

| # | 反馈 | 处理 |
|---|---|---|
| ①(新) | 顶部标题栏不模糊 | 「顶部标题栏磨砂」开关（默认开），backdrop-filter 不糊文字 |
| ②(新) | 要左右滑动开关 | 全部改为滑动式开关（含布尔参数项） |
| ③(新) | 缩放只能调大 | 缩放范围 50–200%，缩小露底色不露白 |
| ④(新) | 参数太多折叠 | 默认显示 8 项 + 展开全部/收起 |
| ⑤(新) | 标签有 &nbsp; | 实体清理 + 空白折叠 |
| ⑥(新) | 无意义输入框 | type:text 参数只读展示；布尔参数用开关 |
| ⑦(新) | 拆解 apk | 见下方研究报告 |
| ⑧(新) | 导出无用 | 已删除导出功能；参数改为只读展示 |
| ⑨(新) | 渲染方案 | 见下方研究报告；新增**视频背景**支持 |

## 渲染可行性研究（①⑨）

- 完整场景（含 Live2D 木偶）只能由专有渲染器完成：`壁纸引擎` App 的原生库 `libscenejni.so`（40MB，内嵌 Chromium + 专有 puppet 渲染）；开源方案 [we-layerd](https://github.com/Aromatic05/we-layerd)（⭐93，Rust）打包了官方渲染器，但需 Linux Wayland 桌面，Termux proot 环境跑不了
- 浏览器端没有成熟的 WE 场景渲染器（[wallgl](https://github.com/lucaschnabel42/wallgl) 是雏形且不支持木偶；pixeltris/wallpaper-engine-web 已消失）
- **可行路径**：外部渲染成视频（壁纸引擎 App 录屏 / 桌面 we-layerd）→ 插件新增**视频背景**（MP4/WebM 存 IndexedDB，`<video>` 循环播放）

## 官方文档（②）

Wallpaper Engine 官方帮助站 [help.wallpaperengine.io](https://help.wallpaperengine.io) 有移动端章节（与 Windows 配对等）；mpkg 容器格式为专有格式，官方未公开文档。

## 13 条反馈落实情况

| # | 反馈 | 处理 |
|---|---|---|
| ① | 提示文案 | 改为「支持 Wallpaper Engine 的 .mpkg 包」 |
| ② | 深色模式「应用」按钮看不清 | 按钮改为固定蓝底白字 + 显式边框/聚焦环 |
| ③ | 磨砂模糊没效果 | 改用 `backdrop-filter` 磨砂（作用于面板背后的背景层，可见效果） |
| ④ | 100% 不透明度时中缝透出背景 | 100% 时隐藏壁纸层 + 帧底色用主题色 |
| ⑤ | 设置挪到左侧导航 | 注册 `settings.section`，与 通用设置/模型/插件/Agent预设 同级 |
| ⑥ | 侧边栏透出开关 | 新增开关，关闭后侧边栏不透明 |
| ⑦ | 动态壁纸模糊 | 新增「轻度锐化」开关（低清 GIF 观感提升） |
| ⑧ | 可调参数修改 | 设置页直接编辑 + 导出修改后的 mpkg（写回 project.json 重建容器） |
| ⑨ | 时间变化 | 按当前时间自动选素材（含多时段素材的壁纸包生效） |
| ⑩ | 背景比例/缩放 | 新增缩放滑杆 100–200% |
| ⑪ | 动态只播开头 | 改为 `<img>` 承载 + 强制 GIF 无限循环补丁 |
| ⑫ | 安全审查 | 源码扫描无敏感内容、无主动网络请求；参考代码均注明许可证 |
| XIII | 静态 mpkg 正常但模糊 | 缩放 + 锐化选项缓解（源图分辨率所限） |

## 安装 / 升级

```bash
# 插件已装入 $DSH_HOME/profiles/node_modules/@local/dsh-mpkg-wallpaper/
# cordis.patch.yml 已注册；重启 dsh web 后浏览器 Ctrl+F5 生效
```

卸载：删除 `cordis.patch.yml` 中的 `mpkg-wallpaper` 行 + 删除插件目录 + 重启。

## 使用

设置 → **壁纸引擎背景**：

| 控件 | 说明 |
|---|---|
| 选择 .mpkg 文件 | 自动取 preview.gif（或按时间取素材）作动态背景 |
| 可调参数 | 编辑布尔/下拉/滑块/文本，点「导出修改后的 mpkg」下载 |
| 图片链接 / 本地图片 | 普通图片或 data:image GIF |
| 面板不透明度 | 50–100%；100% 时背景隐藏、不透缝 |
| 磨砂模糊 | backdrop-filter 磨砂 0–40px |
| 背景缩放 | 100–200% |
| 侧边栏透出壁纸 | 开关；关闭后侧边栏不透明 |
| 轻度锐化 | 提升低清观感；GIF 卡顿就关 |

## 安全说明（⑫）

- **无主动网络请求**：插件不 fetch/XHR/WebSocket；唯一网络行为是用户手动输入的图片 URL 由浏览器加载（与官方示例插件相同）
- **无敏感内容**：源码不含路径、密钥、令牌、个人信息
- **无第三方闭源代码**：仅依赖 DSH 自带 react + 官方 slots/locale 接口
- 参考项目（均开源）：[dsh-bg-image](https://github.com/lyh9712/dsh-bg-image)（MIT，模板）、[unmpkg](https://github.com/aqnya/unmpkg)（GPL-3.0，仅参考 mpkg 二进制格式）、[repkg](https://github.com/notscuffed/repkg)（GPL，仅研究 .tex 格式）、[astc-encoder](https://github.com/ARM-software/astc-encoder)（Apache-2.0，本地解码实验）
- 数据边界：所有解析在浏览器本地完成；localStorage 只存背景图 data URL 与参数编辑

## 限制（诚实说明）

这两个示例 mpkg 都是 **Live2D 场景类壁纸**（木偶模型 + shader + 粒子），完整场景只能在壁纸引擎 App 渲染。插件取用的 `preview.gif` 是作者生成的动画预览（256×256 / 187×187），全屏会偏模糊——缩放/锐化只能缓解；「时间变化」等参数修改后需导出 mpkg 到 App 才真正生效，浏览器端无法渲染改后的画面。

## 文件结构

```
dsh-mpkg-wallpaper/
├── package.json      # dsh.client 声明
├── lib/
│   ├── index.js      # 宿主端空实现
│   └── client.js     # 浏览器端：mpkg 解析/重建 + 设置页 + 背景 DOM + localStorage
└── README.md
```

## GitHub 发布说明

### 可移植性（在他人的设备上也能用）

- 无绝对路径、无本机端口、无环境专属配置；依赖仅 DSH 自带 react + 官方 slots/locale 接口
- **自定义导航图标**：`lib/client.js` 里的 `NAV_ICON` 常量（默认是自绘的"风景画"SVG，无商标）可替换——改成你自己的图标即可（20×20，推荐 SVG data URL 或 base64 PNG）
- 素材库网页（`http://127.0.0.1:8090/素材库.html`）是独立工具，不随插件发布；需要时自行用 `python3 -m http.server 8090 -d 素材目录` 启动

### 包含的逆向工具（tools/）

| 工具 | 用途 |
|---|---|
| `unmpkg.py` | mpkg 容器解析/提取（PKGM0014/0018） |
| `tex2png.py` | TEXV0005 纹理解码（DXT5/R8 等） |
| `mdl_explorer.py` | .mdl 结构探索（块标签/网格/浮点区段） |
| `xref.py` | wallpaper64.exe 字符串 xref + 反汇编（capstone） |
| `MDL-格式分析笔记.md` | .mdl 格式逆向进展（容器/网格已破解，骨骼=JSON，动画待续） |

### 安全说明

- 纯客户端、无网络请求（除用户主动填写的图片 URL）、无命令执行面
- 导入文件按魔数校验（拒绝 SVG/HTML/脚本伪装）、URL 协议白名单、referrerPolicy=no-referrer
- 恶意 mpkg 无法触达宿主文件系统（浏览器沙箱内解析）

### 壁纸格式研究摘要（供其他开发者）

- **mpkg**：PKGM0014（视频类：mp4+gif+json）/ PKGM0018（场景类：scene.json+tex+mdl+shader）
- **tex**：TEXV0005，格式 5=DXT 家族，格式 34=内嵌 MP4 视频纹理（customize 壁纸的 4K 动画直接在里面）
- **mdl**：MDLV00xx 块容器；网格=8 float/顶点；MDLS0003/0004 含 JSON 骨骼姿态；MDLA=动画
