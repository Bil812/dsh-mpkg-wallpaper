// @local/dsh-mpkg-wallpaper —— 纯客户端插件（mpkg 动态背景 + 普通背景图）。
// 服务端入口：无任何 host-side 行为，仅为满足 cordis loader 的插件解析。
// 前端逻辑在 ./client.js（package.json 的 "./client" 子路径导出）。
function apply() {}
export { apply };
